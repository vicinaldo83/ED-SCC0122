/*DicToBin.c*/

#include <stdio.h>
/*
    #define PilhaSize 100

    typedef int items
    typdef int topo

    struct { 
        
    }

*/


/*  
    import Pilha
    main {
        pilha = Pilha();
        int decimal = input();

        while (decimal > 0){
            pilha.push(decimal % 2);
            decimal = decimal / 2;
        }

        print(pilha.flatten());
        pilha.destroy();
    }

    pilha.push():... adiciona item a pilha
    pilha.faltten() {
        out
        while (!pilha.vazia()) {
            out += (str)pilha.pop()
        }
    }
    pilha.pop():.. retorna e remove primeiro item
    pilha.destroy():... autoexplicativo
    pilha.vaiza or size:... verifica o tamnho da lista
*/



int main() {

}